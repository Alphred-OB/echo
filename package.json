{
  "name": "echo-auth",
  "version": "0.1.0",
  "description": "Echo — ultrasonic passwordless authentication (CY384 artefact)",
  "main": "src/server.js",
  "scripts": {
    "start": "node --experimental-sqlite src/server.js",
    "test": "node --experimental-sqlite tests/test-flow.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.0",
    "nodemailer": "^9.0.3",
    "ws": "^8.17.0"
  }
}
